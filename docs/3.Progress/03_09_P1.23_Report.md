# Báo cáo P1.23: Toolbar tối giản với SVG Kawaii Lite icons

- **Ngày thực hiện:** 2026-09-15
- **Phạm vi:** Epic 4 — Undo/Redo & Toolbar (`FR-TB-01`).
- **Trạng thái:** Hoàn thành, build OK, tests pass.

---

## 1. Thiết kế đã rà soát

| File | Nội dung |
| --- | --- |
| `docs/2.Design/12_UIUX_Mock_Penpot/12_01_DesignTokens.md` | Bảng màu Kawaii Claymorphism đã chuẩn hóa: Core Palette, Semantic Groups, Functional Colors. |
| `docs/2.Design/12_UIUX_Mock_Penpot/12_06_Icon_Asset_Production_Guide.md` | Quy trình tạo icon SVG; yêu cầu 512×512 px PNG nền trắng, viewBox 32×32. |
| `docs/2.Design/12_UIUX_Mock_Penpot/assets/icon/kawaii_lite/` | Bộ icon SVG Kawaii Lite tự vẽ, đồng bộ màu semantic. |

---

## 2. Code đã triển khai

| File | Thay đổi chính |
| --- | --- |
| `src/FShot.UI/SkiaCanvas/ToolbarIcons.fs` (mới) | Định nghĩa path data cho 10 icon (Selection, Pencil, Line, Arrow, Rectangle, Circle, Marker, Text, Pixelate, Icon) theo viewBox 32×32; hàm `fillColor`/`strokeColor` map màu theo tool. |
| `src/FShot.UI/SkiaCanvas/CaptureCanvas.axaml.fs` | Thêm `open FShot.UI.SkiaCanvas`; đổi `tools` từ tuple `(ToolKind, label)` sang `ToolKind`; vẽ toolbar bằng icon qua `StreamGeometry.Parse`; áp dụng màu nền kem `#FFFDF9`, viền xám `#E2E8F0`, bóng đổ mềm, active accent `#38BDF8`. |
| `src/FShot.UI/FShot.UI.fsproj` | Thêm `ToolbarIcons.fs` vào danh sách compile trước `CaptureCanvas.axaml.fs`. |

---

## 3. Hành vi

- Toolbar hiển thị 10 icon hình học Kawaii Lite thay vì chữ đại diện.
- Tool đang active có nền xanh `#38BDF8` và viền xanh dày 2px, bo góc 6px.
- Toolbar vẫn bám quanh capture region, tự động chuyển hướng bottom/top/left/right khi vùng chọn sát mép.
- Hit-test giữ nguyên logic cũ, click đúng tool vẫn dispatch `SelectTool`.
- Phím tắt tool (`P`, `L`, `A`, ...) vẫn hoạt động.

---

## 4. Kết quả kiểm thử

```text
FShot.Core.Tests.dll        Passed: 167 / 167
FShot.Rendering.Skia.Tests  Passed:  21 /  21
FShot.UI.Tests.dll          Passed:   1 /   1
```

---

## 5. Nợ kỹ thuật

- **Hover visual** (nền vàng bơ nhạt `#FEF9C3`, scale 105%) chưa được triển khai vì cần theo dõi vị trí chuột riêng trong `CaptureCanvas`. Thuộc cải tiến UI sau MVP.
- Các icon hiện tại là **Kawaii Lite** (hình học đơn giản, không có mặt cười/bóng đổ/highlight specular đầy đủ). Sau này có thể thay bằng icon AI chuẩn Kawaii chỉ cần giữ nguyên tên file `ToolbarIcons.fs` và chuỗi path data.
- Toolbar chưa hiển thị nút **Undo/Redo/Save/Copy/Cancel**; các action này hiện chỉ dùng phím tắt, sẽ bổ sung trong Epic 5.

---

## 6. Tiếp theo

- **Epic 5: Xuất dữ liệu & CLI** (P1.24–P1.30): Save `Ctrl+S`, Copy `Ctrl+C`, CLI args, config path.
- Có thể bổ sung hover visual và nút action trên toolbar trong quá trình làm Epic 5 nếu cần.
