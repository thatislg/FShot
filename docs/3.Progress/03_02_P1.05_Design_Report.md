# Báo cáo thiết kế P1.05 — Lớp phủ tối mờ Skia

**Ngày:** 2026-09-13  
**Phase:** 1 — MVP Core  
**Task:** P1.05 Lớp phủ tối mờ Skia (`FR-SEL-01`) ngoài vùng chọn.

---

## 1. Mục tiêu

Triển khai lớp đồ họa nửa trong suốt phủ lên toàn bộ màn hình chụp, trừ vùng chọn đang active. Mục tiêu là giúp người dùng tập trung vào vùng sẽ được xuất, đồng thời vẫn nhìn thấy nội dung màn hình bên dưới ở mức độ mờ.

---

## 2. Các file đã hoàn thiện

| File | Nội dung chính | Trạng thái |
|------|---------------|------------|
| `docs/2.Design/03_Selection/03_07_OverlayDimming.md` | Thiết kế dimming: mục đích, thuật toán Clear, tọa độ/scaling, trạng thái hiển thị, cấu hình, tích hợp SceneComposer. | Hoàn thiện |
| `src/FShot.Rendering.Skia/Renderers/DimmingRenderer.fs` | Renderer chuyên trách: vẽ toàn màn hình tối rồi khoét lỗ vùng chọn bằng `SKBlendMode.Clear`. | Hoàn thiện |
| `src/FShot.Rendering.Skia/Renderers/SceneComposer.fs` | Điều phối: gọi `DimmingRenderer` sau screenshot và trước selection border. | Hoàn thiện |
| `tests/FShot.Rendering.Skia.Tests/DimmingTests.fs` | Kiểm thử render dimming: phủ ngoài vùng chọn, opacity 0.5, opacity 0, sự khác biệt inside/outside. | Hoàn thiện |

---

## 3. Quyết định thiết kế chính

### 3.1 Thuật toán

Sử dụng phương pháp **Clear blend mode** thay vì clip path vì:

- Đơn giản, ít state trên canvas.
- Hiệu năng tốt với Skia.
- Dễ kiểm thử bằng cách đọc pixel.

Bước thực hiện:

1. Vẽ hình chữ nhật phủ toàn màn hình với màu đen alpha = opacity.
2. Vẽ hình chữ nhật vùng chọn với `BlendMode = Clear` để xóa phần tối trong vùng chọn.

### 3.2 Tọa độ và scaling

Tất cả tọa độ domain là logical. Renderer chuyển sang physical pixels bằng `CaptureResult.ScaleFactor`:

```text
physicalX = round(logicalX * scaleFactor)
physicalY = round(logicalY * scaleFactor)
physicalWidth = round(logicalWidth * scaleFactor)
physicalHeight = round(logicalHeight * scaleFactor)
```

### 3.3 Cấu hình

Giá trị mặc định:

- `DimmingColor`: đen `#000000`.
- `DimmingOpacity`: 0.5 (50%).

Opacity có thể điều chỉnh trong tương lai qua Settings.

### 3.4 Thứ tự vẽ trong SceneComposer

1. Screenshot bitmap.
2. **Dimming overlay**.
3. Selection border.
4. Resize handles.
5. Annotations.
6. Preview.
7. Toolbar / TextInput.

---

## 4. Kết nối với các phần khác

- **SceneComposer:** điều phối layer vẽ.
- **Selection:** cung cấp `Selection.Bounds`.
- **CaptureResult:** cung cấp `VirtualBounds` và `ScaleFactor`.
- **ConfigSnapshot:** trong tương lai sẽ cung cấp opacity tùy chỉnh.

---

## 5. Tiến độ

- [x] Hoàn thiện thiết kế `03_07_OverlayDimming.md`.
- [x] Triển khai `DimmingRenderer.fs`.
- [x] Cập nhật `SceneComposer.fs`.
- [x] Viết tests `DimmingTests.fs`.
- [ ] Tích hợp vào UI layer để hiển thị thực tế (sẽ làm sau khi hoàn thành toàn bộ Epic 2).

---

## 6. Kết quả kiểm thử

```text
dotnet test tests/FShot.Rendering.Skia.Tests: 4 passed, 0 failed.
```

Các test bao gồm:

- Pixel ngoài vùng chọn bị tối.
- Pixel trong vùng chọn khác biệt (đã được clear).
- Opacity 0.5 tạo màu xám tối hơn nền trắng.
- Opacity 0 giữ nguyên nền trắng.

---

## 7. Next steps

1. Chuyển sang P1.06: Kéo bounding box tự do — kiểm thử đầy đủ `Selection.StartSelecting`/`UpdateSelecting`/`FinishSelecting`.
2. Sau đó P1.07: 8 resize handles.

---

*Báo cáo này được lập sau khi hoàn thiện P1.05, đã kiểm tra và pass toàn bộ test.*
