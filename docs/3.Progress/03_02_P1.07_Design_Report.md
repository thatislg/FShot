# Báo cáo thiết kế P1.07 — 8 điểm neo co giãn vùng chọn

**Ngày:** 2026-09-13
**Phase:** 1 — MVP Core
**Task:** P1.07 8 điểm neo co giãn vùng chọn (`FR-SEL-03`).

---

## 1. Mục tiêu

Đảm bảo vùng chọn đã tạo có thể được co giãn thông qua 8 điểm neo: 4 góc và 4 cạnh. Mỗi handle phải đặt đúng vị trí, hit-test dễ dàng với tolerance, và khi kéo phải thay đổi đúng cạnh/cạnh của vùng chọn theo công thức đã định nghĩa. Kết quả cuối cùng phải được clamp vào capture area và tuân thủ kích thước tối thiểu.

---

## 2. Các file đã hoàn thiện

| File | Nội dung chính | Trạng thái |
|------|---------------|------------|
| `docs/2.Design/03_Selection/03_03_ResizeHandles.md` | Bổ sung công thức hit-test, thứ tự ưu tiên, công thức resize 8 handles, ràng buộc clamp/min-size. | Hoàn thiện |
| `src/FShot.Core/Domain/Selection.fs` | Không thay đổi API; `ResizeHandle`, `HandleCenters`, `HitTestHandle`, `StartResizing`, `UpdateResizing` đã đáp ứng thiết kế. | Khớp thiết kế |
| `src/FShot.Core/Geometry/Operations.fs` | `rectInflate` và `rectContainsPoint` hỗ trợ `hitHandle`. | Khớp thiết kế |
| `src/FShot.Core/State/OverlayState.fs` | Đã chuyển `Selected` → `Resizing` khi `HitTestHandle` trả về handle. | Khớp thiết kế |
| `tests/FShot.Core.Tests/Domain/SelectionTests.fs` | Thêm tests cho 8 handles, hit-test tolerance, ưu tiên góc, resize quá nhỏ bị hủy, resize ra ngoài capture area, `UpdateResizing` bỏ qua khi không `Resizing`. | Pass |

---

## 3. Quyết định thiết kế chính

### 3.1 Vị trí 8 handle

Cho vùng chọn `Bounds = (L, T, W, H)`, các handle đặt tại:

```text
TopLeft      = (L, T)
Top          = ((L + R) / 2, T)
TopRight     = (R, T)
Left         = (L, (T + B) / 2)
Right        = (R, (T + B) / 2)
BottomLeft   = (L, B)
Bottom       = ((L + R) / 2, B)
BottomRight  = (R, B)
```

Trung tâm cạnh dùng `(left + right) / 2` thay vì `X + Width / 2` để tránh sai số làm tròn và duy trì đối xứng.

### 3.2 Hit-test handle

Mỗi handle là hình vuông kích thước `s × s` tại trung tâm, được mở rộng đều `tolerance` theo 4 hướng để tạo vùng hiệu lực:

```text
E.Left   = Center.X - s/2 - tolerance
E.Top    = Center.Y - s/2 - tolerance
E.Width  = s + 2 * tolerance
E.Height = s + 2 * tolerance
```

Nếu con trỏ nằm trong `E`, handle được chọn.

### 3.3 Thứ tự ưu tiên

`HandleCenters` được duyệt theo thứ tự cố định: TopLeft, Top, TopRight, Left, Right, BottomLeft, Bottom, BottomRight. Handle đầu tiên mà con trỏ nằm trong vùng hiệu lực được chọn, ưu tiên góc trước cạnh.

### 3.4 Công thức resize

Khi handle đang kéo, vùng mới tính từ `OriginalBounds` và vị trí chuột `(mx, my)`:

| Handle | Công thức |
|--------|-----------|
| TopLeft | `X = mx`, `Y = my`, `W = right - mx`, `H = bottom - my` |
| Top | `Y = my`, `H = bottom - my` |
| TopRight | `X = orig.X`, `Y = my`, `W = mx - X`, `H = bottom - my` |
| Left | `X = mx`, `W = right - mx` |
| Right | `W = mx - X` |
| BottomLeft | `X = mx`, `Y = orig.Y`, `W = right - mx`, `H = my - Y` |
| Bottom | `H = my - Y` |
| BottomRight | `W = mx - X`, `H = my - Y` |

### 3.5 Ràng buộc khi resize

- Trong khi kéo, `Bounds` được `ApplyConstraints` để nằm trong capture area.
- Không ép cứng min-size trong lúc kéo để tránh giật cục.
- Sau khi thả (`FinishInteraction`), nếu `Bounds` không đạt `MinWidth`/`MinHeight`, vùng bị hủy về `Idle`.

---

## 4. Kết nối với các phần khác

- **OverlayState:** nhận `PointerPressed` khi `Selected`, gọi `HitTestHandle`. Nếu trả về handle, chuyển sang `Resizing`; nếu không, xử lý move/annotating/new-selection.
- **Geometry.Operations:** `rectInflate` mở rộng handle bounds; `rectContainsPoint` kiểm tra point-in-rect.
- **SelectionRenderer (tương lai):** dùng `HandleCenters` để vẽ 8 handle lên canvas.

---

## 5. Tiến độ

- [x] Rà soát `docs/2.Design/03_Selection/03_03_ResizeHandles.md`.
- [x] Đảm bảo `Selection.HandleCenters`, `HitTestHandle`, `StartResizing`, `UpdateResizing`, `FinishInteraction` hỗ trợ đủ 8 handles.
- [x] Kiểm thử hit-test tolerance và resize theo 8 hướng.
- [x] Kiểm thử kích thước tối thiểu sau khi resize.

---

## 6. Kết quả kiểm thử

```text
dotnet test tests/FShot.Core.Tests --no-restore --verbosity minimal
Passed!  - Failed: 0, Passed: 99, Skipped: 0, Total: 99, Duration: 89 ms

dotnet test --no-restore --verbosity minimal
FShot.Core.Tests:       99 passed
FShot.Rendering.Skia.Tests: 4 passed
```

Các test mới bao gồm:

- Resize theo 8 handles: TopLeft, Top, TopRight, Left, Right, BottomLeft, Bottom, BottomRight.
- Hit-test cả 8 handle tại trung tâm.
- Hit-test tolerance mở rộng vùng hiệu lực.
- Hit-test ưu tiên góc trước cạnh.
- Resize quá nhỏ bị hủy khi hoàn tất.
- Resize ra ngoài capture area bị clamp.
- `UpdateResizing` bỏ qua khi không ở trạng thái `Resizing`.

---

## 7. Next steps

1. Chuyển sang **P1.08** — Kéo rê di chuyển toàn bộ vùng chọn (`FR-SEL-04`).
2. P1.08 chủ yếu kiểm thử clamp khi move và tích hợp trong `OverlayState`.

---

*Báo cáo này được lập sau khi hoàn thiện P1.07, đã kiểm tra và pass toàn bộ test.*
